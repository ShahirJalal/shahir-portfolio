spring:
  datasource:
    url: jdbc:h2:mem:portfolio-test;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

app:
  cors:
    allowed-origins: http://localhost:4300
